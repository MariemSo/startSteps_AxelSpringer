//! Task 1: Greeting Function:
    // ● Write a JavaScript function named greet that takes a parameter name (string)
    // and returns a personalized greeting message.
    // ● Call the greet function with a sample name and print the result to the
    // console.

        function greet(name){
            return `Hello, ${name}. Hope You have an Amazing Day 😁`
        }

        console.log(greet("Miou"));

//! 2.Task 2: Addition Function:
    // ● Write a JavaScript function named add that takes two parameters (numbers)
    // and returns their sum.
    // ● Call the add function with sample numbers and print the result to the
    // console.

        function add(a,b){
            return `The sum of (${a} and ${b}) is ${a+b}`
        }

        console.log(add(3,4))

//! 3.[Bonus] Task 3: Multiplication Function:
    // ● Write a JavaScript function named multiply that takes two parameters
    // (numbers) and returns their product.
    // ● Call the multiply function with sample numbers and print the result to the
    // console.
    
        function multiply(a,b){
            return `The result of (${a} * ${b}) is ${a*b}`
        }

        console.log(multiply(2,4))